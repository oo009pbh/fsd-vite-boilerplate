/**
 * @description vite.config 에서 사용되는 환경변수
 * - 이미지 path 정보를 assign 할 때 필요한 정보
 * @type {string}
 */
const env = import.meta.env.BASE_URL;

// prettier-ignore
export const Images = {
  // icon
  // Bookmark: `${env}/assets/img/ico/ic-bookmark.svg`,
};
