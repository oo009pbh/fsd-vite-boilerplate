import { ViewConfigType } from '@typings/common/molecule';

export interface UseDevSampleDetailType {
  detailNo: string;
}

export interface DevSampleDetailType {
  config: ViewConfigType;
  detailNo: string;
}
